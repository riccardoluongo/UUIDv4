An implementation of UUIDv4 in C only using the standard library and getrandom().

Originally made for multi-range responses in my http server; I decided to make this a separate repository for those looking for a decent implementation that should work anywhere getrandom() is supported.
